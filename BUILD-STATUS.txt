TypeGuru Pro - Phase 5 Build Status (v0.5.0)
=============================================
PHASE 5 = Government Exam Mode + Certificates.
WHAT WAS DONE IN THIS ENVIRONMENT:
  - All Phase 5 source files authored (see MANIFEST.txt).
  - Exam data uses a SEPARATE IndexedDB database ("typeguru-exam-db") so the
    existing main database schema did NOT need risky edits.
WHAT WAS **NOT** RUN HERE (honest):
  - npm ci / npm install : NOT run (no network access in this sandbox).
  - npm run typecheck     : NOT run.
  - npm run build         : NOT run.
  - Windows installer/EXE  : NOT built.
  - App was NOT launched or smoke-tested.
WHY: The build sandbox has no internet, so dependencies could not be installed
and the TypeScript/Vite toolchain could not run. Nothing was faked.
HONESTY NOTES:
  - Exam templates (SSC/RRB/CPCT) are EDITABLE format presets with illustrative
    target numbers. Passages are ORIGINAL practice text, not copied from any exam.
  - Certificates are generated ONLY from real passed in-app results. No fake data.
  - Certificates are practice certificates, NOT official government documents.
APPLY ORDER: Apply the Phase 4 update first, then this Phase 5 update. The new
App.tsx references Phase 4 pages (Font Setup, Mapping Validator, Converter).
HOW TO VERIFY / BUILD YOURSELF:
  1. npm ci
  2. npm run typecheck
  3. npm run build
  4. Windows EXE: push to GitHub, run the "Build Windows EXE" Action, download
     the TypeGuruPro-Windows artifact. (Unsigned build.)
