// Superseded: the tested converter lives in src/converter/krutiDevCore.ts.
// (The old table here had wrong entries, e.g. "," mapped to ऋ instead of ए,
// and did not reorder ि or reph.) Kept as a re-export so old imports still work.
export { krutiDevToUnicode, unicodeToKrutiDev } from "../converter/krutiDevCore";
